#include "main.h"
/**
 * *_strchr - check the occurance of a char
 * @s: string
 * @c: char
 * Return: null if char doesn't occur or char if it ocuur
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
