#include <stdio.h>
/**
 *main - it prints an array of characters
*Return: return 0
*/
int main(void)
{
	char str[50] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
