/**
 * _isupper - check if a number is upppercase or lowercase
 * @c: is an intiger
 * Return: return 1 if uppercase else 0
 */

int _isupper(int c)
{
	if (c > 'a' && x < 'z')
	{
		return (1);
	}

	else
	{
		return (0);
	}
}
